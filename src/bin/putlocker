#!/usr/bin/env ruby

require_relative '../lib/service/cli/command_point'

module Service
  module PutLocker
    ##
    # Service CLI interface
    #
    module Cli
      Service::Cli::CommandPoint.new.run if $0 == __FILE__
    end
  end
end
